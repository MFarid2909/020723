class Program
{
    static void Main(string[] args)
    {
        int[] arr = { 1, 2, 3, 5, 6, 7 };
        int sum = 0;
        for (int i = 0; i < arr.Length; i++)
        {
            sum += arr[i];
            Console.WriteLine(arr[i]);
        }
        Console.WriteLine(sum);
    }

}
